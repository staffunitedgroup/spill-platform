import { PageIntro } from "@/components/page-intro";
export default function Page() { return <PageIntro eyebrow="Across the SPILL network" title="What’s on" description="Discover events, SPILL 42, live broadcasts, highlights, and what is happening this week. Filters for location, date, event type, and ticket type will follow with the event database." />; }
